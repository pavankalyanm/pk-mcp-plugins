[project]
name = "mails-bridge"
version = "0.2.0"
description = "Multi-account email MCP server for Claude — supports Gmail, Outlook, Yahoo, iCloud, and any IMAP/SMTP provider"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.2",
]
